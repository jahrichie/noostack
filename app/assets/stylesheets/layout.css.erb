/**
 * For modern browsers
 * 1. The space content is one way to avoid an Opera bug when the
 *    contenteditable attribute is included anywhere else in the document.
 *    Otherwise it causes space to appear at the top and bottom of elements
 *    that are clearfixed.
 * 2. The use of `table` rather than `block` is only necessary if using
 *    `:before` to contain the top-margins of child elements.
 */
.cf:before,
.cf:after {
    content: " "; /* 1 */
    display: table; /* 2 */
}

.cf:after {
    clear: both;
}

/**
 * For IE 6/7 only
 * Include this rule to trigger hasLayout and contain floats.
 */
.cf {
    *zoom: 1;
}


/*
MYSTACK
by rich oreilly
so digital design and dev
sodigit.al
*/

body {
 background:#F3F3F3;
}

table {width: 100%;}

th {
  font-weight: bold;
  text-transform: uppercase;
}

#view-wrapper {
  width: 1000px;
  margin: 0 auto;
}
.full-width-top {
  background: #000;
  height: 40px;
}

.header-nav {
  display: block;
  color: #FFF;
  width: 100%; 
  height:38px;
  padding:25px 0;
  background:url(<%= asset_path 'layout/logo.png' %>) center no-repeat;
  background-size: 50%;
  /*border: 1px solid #000;*/
}

.add-link a {
  margin-left:230px;
}

.search {
  float:right;
  margin-right:20px;
 
 
}

.search input {
  border-radius: 4px;
  border:0;
  height: 30px;
  width:180px;
  margin-top: 4px;   
  text-indent: 10px; 
}


.make-or-search li a {
  display: block;
  float: left;
  margin-right: 10px;
  background: #000;
   /*background:url(<%= asset_path 'nav/tweed.png' %>) no-repeat;*/
  color: white;
  padding: 29px;
  margin-top: -37px;
  -webkit-transition:all 0.250s ease-in-out;
  -moz-transition:all 0.250s ease-in-out;
  -o-transition:all 0.250s ease-in-out;
  transition:all 0.250s ease-in-out;

}

.make-or-search li a:hover {
  background: yellow;
  color: black;
}

ul.make-or-search li {
  display: block;
}


.field input,.field label {
  float: left;
  margin: 20px;
}

.field input {
  width: 300px;
  height: 50px;
  padding: 5px;
}

a {
  -webkit-transition:all 0.250s ease-in-out;
  -moz-transition:all 0.250s ease-in-out;
  -o-transition:all 0.250s ease-in-out;
  transition:all 0.250s ease-in-out;
}

.field_with_errors input,.field_with_errors textarea {
  border: 1px solid #CB5E7B;
}